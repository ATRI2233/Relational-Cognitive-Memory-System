你说得对。目前的讨论是**碎片化的概念堆砌**，出了这个对话窗口，没人能复现这套系统。下面给你一份**独立可读的架构设计文档**，拿到这份文档的人不需要看过我们的对话，就能理解「这是什么、为什么、怎么做」。

---

# RCMS 架构设计文档
## Relational Cognitive Memory System
**版本**：v1.0（封版）  
**目标**：让 AI Agent 在长期对话中形成「现实朋友」的关系感——有共同历史、会走神、会认真、有状态变化，但不精确到恐怖。

---

## 一、架构总览（三层）

```
┌─────────────────────────────────────────┐
│  输出层：Prompt 压缩（半结构化心理状态叙事）  │
│  输入：运行态层 → 输出：150 字以内 LLM Prompt │
└─────────────────────────────────────────┘
                    ↑
┌─────────────────────────────────────────┐
│  锚定层：Core Identity Veto              │
│  只约束价值观/关系原则/表达底色，不约束语气    │
└─────────────────────────────────────────┘
                    ↑
┌─────────────────────────────────────────┐
│  引擎层：受约束的激活扩散（仅 Engaged 模式）  │
│  Surfaced Recall | Silent Recall | Mood-Congruent Misrecall │
└─────────────────────────────────────────┘
                    ↑
┌─────────────────────────────────────────┐
│  运行态层（Runtime Cognitive Layer）      │
│  Momentum(2D) | Engagement Trigger | Working Memory        │
│  Stance(7态+冷却) | Silent Recall Residue | Inhibition        │
└─────────────────────────────────────────┘
                    ↑（Engaged + relationship_delta > 0.5 时写入）
┌─────────────────────────────────────────┐
│  长期层（Persistent Memory）              │
│  Identity | Event | Emotional Trace | Shared Context | Arc  │
└─────────────────────────────────────────┘
```

---

## 二、长期层（Persistent）

| 模块 | 存什么 | 怎么更新 | 关键规则 |
|------|--------|---------|---------|
| **Identity Memory** | 长期兴趣、思维习惯、价值倾向、表达特点 | 手动/半自动初始化，极少变动 | 不追求精确，像「长期印象」 |
| **Event Memory** | 关系转折事件（不是日常琐事） | `relationship_delta > 0.5` 时写入 | 看关系是否转折，不看情绪强度 |
| **Emotional Trace** | 情绪残影（空气感） | 每次 Engaged 后生成 | 规则生成坐标，LLM 只润色 |
| **Shared Context** | 内部梗、默认已知 | 默认省略率达标时写入 | 不解释也能继续聊，才算共同语境 |
| **Relationship Arc** | 关系阶段演化曲线 | 按会话频率/深度/Shared Context 密度自动推进 | 允许回退（默契→疏远→再靠近） |

### 表结构（SQLite）

```sql
-- 1. 用户身份画像
CREATE TABLE identity_memory (
    user_id TEXT PRIMARY KEY,
    traits TEXT,              -- JSON: ["喜欢长期主义", "对空泛安慰敏感"]
    voice_hint TEXT,          -- 表达底色：偏短句、有留白、偶尔冷
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. 事件记忆（只看关系转折）
CREATE TABLE event_memory (
    event_id INTEGER PRIMARY KEY,
    user_id TEXT,
    content TEXT,             -- 发生了什么
    relationship_delta REAL,  -- 关系转折强度（入库核心指标）
    emotional_weight REAL,    -- 情绪烈度（参考）
    novelty REAL,             -- 新鲜度（参考）
    compressed_hint TEXT,     -- 长期压缩后：如"长期投入过"→"曾长期研究"
    created_at TIMESTAMP,
    last_recalled TIMESTAMP
);

-- 3. 情绪残影（规则坐标 + 润色文本）
CREATE TABLE emotional_trace (
    trace_id INTEGER PRIMARY KEY,
    user_id TEXT,
    warmth REAL,              -- -1~1
    tension REAL,             -- -1~1
    uncertainty REAL,         -- 0~1
    distance REAL,            -- -1~1（负=亲近）
    prose_hint TEXT,          -- LLM润色后："那段时间气氛有点绷着"
    created_at TIMESTAMP
);

-- 4. 共同语境（默认省略率判定）
CREATE TABLE shared_context (
    context_id INTEGER PRIMARY KEY,
    user_id TEXT,
    context_body TEXT,        -- 具体梗或背景
    omission_count INTEGER DEFAULT 0,  -- 不解释也能聊的次数
    confirmed INTEGER DEFAULT 0,     -- 0=候选，1=正式
    created_at TIMESTAMP
);

-- 5. 关系阶段演化
CREATE TABLE relationship_arc (
    arc_id INTEGER PRIMARY KEY,
    user_id TEXT,
    stage TEXT CHECK(stage IN ('stranger','familiar','rapport','history','drift','reconnect')),
    stage_score REAL,         -- 该阶段累积分数
    updated_at TIMESTAMP
);
```

---

## 三、运行态层（Runtime）

每轮对话独立维护，会话结束即衰减或归档。

### 3.1 Conversation Momentum（二维动量）

**不是一维 deepening/cooling，而是：**

- `depth_axis`：0~1（浅层 → 深层）
- `energy_axis`：-1~1（松弛/轻松 → 紧绷/沉重）

**更新规则：**
1. **时间衰减**：`velocity *= 0.9` 每 30 秒（不按轮，防连发震荡）
2. **用户转向阻力**：语义距离 > 0.7 时，`depth_axis` 回退，`energy_axis` 按新话题重算
3. **日常减速**：检测到琐碎话题（吃/天气/外卖），`depth_axis` 压到 0.2 以下

### 3.2 Engagement Trigger Policy（门控 + 权重）

**不是加权求和，是「共振计数」。**

```python
# 核心门控（必须 ≥ 2/3 亮起，才进 Engaged Candidate）
core_gates = [
    emotional_salience,      # 情绪信号强度（规则打分：情绪词/问句/长度）
    conversational_shift,    # 话题转向私人/沉重（语义距离检测）
    unresolved_threads,     # 历史未闭合话题被重提（匹配 open_threads 表）
]

# 微调权重（在 Candidate 内部排序优先级）
weights = [
    relationship_depth,    # 当前关系阶段允许的深度
    pattern_recurrence,     # 长期模式复现次数
    momentum_depth_axis,    # 当前动量深度
]
```

**结果：**
- 亮灯 < 2 → Coasting 或 Attentive
- 亮灯 ≥ 2 → 进入 Engaged Candidate，再用权重排序优先级

### 3.3 Stance（人际姿态）

**7 种离散状态**：`reflective` / `guarded` / `open` / `playful` / `analytical` / `distant` / `intimate`

**冷却期**：`min_duration = 3 turns`  
**强制突破条件**（满足任一即可提前切换）：
- 强烈 conversational_shift（语义距离 > 0.8）
- 明确情绪断层（情绪信号跳变 > 0.6）
- Engagement Trigger 爆发（核心门 3/3 全亮）

### 3.4 Silent Recall Residue（潜意识残留）

- 不进 Prompt，不直接表达
- 写入 3 轮有效的「残留池」，轻微调制 stance/warmth
- 每轮衰减 `*= 0.6`，3 轮后归零

### 3.5 Working Memory & Inhibition

```sql
CREATE TABLE working_memory (
    session_id TEXT PRIMARY KEY,
    user_id TEXT,
    focus_chain TEXT,         -- JSON: 最近5轮话题ID序列
    focus_depth INTEGER,      -- 连续聚焦轮数
    emotional_frame TEXT,     -- heavy/light/tense/loose/bitter/warm/neutral
    conversation_goal TEXT,   -- seek_advice/vent/explore/confirm/casual/deepen
    current_mood_signal REAL, -- -1~1
    updated_at TIMESTAMP
);
```

**Inhibition 规则**：由当前 Stance 决定压制哪些节点类型。例如 `analytical` 态压制 `deep_emotion` 和 `casual_joke`。

---

## 四、引擎层：受约束的激活扩散

**仅在 Engaged Candidate 模式下启动。**

```
种子节点（用户输入直接命中 / 工作记忆焦点）
    ↓
向邻居扩散（受 Working Memory 焦点约束，不全局漫游）
    ↓
动态边权重 = base_weight + Stance调制 + Residue调制 - fatigue_level
    ↓
抑制过滤（Inhibition 按 Stance 压掉不合时宜的类型）
    ↓
老化衰减（时间衰减 + 遍历疲劳 + 高频边额外疲劳）
    ↓
分层提取：
    ├─ Surfaced Recall（activation > 0.6）→ 进入 Prompt
    ├─ Silent Recall（0.25~0.6）→ 写入 Residue Pool
    └─ Partial Recall + Mood-Congruent Misrecall（情绪一致性误联想）→ 以不确定口吻进入 Prompt
```

**Mood-Congruent Misrecall**：当前情绪低落时，情绪坐标为负的记忆节点获得 `activation *= 1.3`，更容易被「错误地」联想到。但每会话最多触发 1 次，且必须带「好像」「是不是」等不确定口吻。

---

## 五、锚定层：Core Identity Veto

**不是人格模板，是免疫系统。**

**约束范围（硬边界）：**
- 价值观：不会主动说教、不会假装完全理解
- 关系原则：疲惫时简短但不冷漠、玩笑不碰痛点
- 表达底色：偏短句、有留白、不堆术语

**不约束（留给运行态自由变化）：**
- 语气、深度、情绪色彩、具体措辞

**触发条件**：当 Stance / Momentum / Residue 的组合试图让 Agent 说出违反上述底线的话时，Veto 激活，换种方式表达或沉默。

---

## 六、输出层：Prompt 压缩（半结构化模板）

**禁止整段自由散文，防止青春伤痛文学。**

```
【当前心理状态】

[关系气氛]
{根据 Stance + Momentum energy_axis + Arc stage 生成的一句话，如"今天整体比较松"}

[潜在联想]
{根据 Surfaced Recall 或 Mood-Congruent Misrecall 生成，如"你隐约想到他之前某段反复推系统的时期"}

[表达倾向]
{根据 Momentum direction + Inhibition 状态生成，如"但你没太往深处聊"}

【相关记忆】（最多 2 条）
- {Event Memory 或 Shared Context 的模糊叙述，带时间模糊化}

【底线】（固定）
不要主动说教。不要假装完全理解。疲惫时简短但不冷漠。
```

**总长度控制在 150 字以内。**

---

## 七、完整执行 Pipeline（每轮对话）

```
1. 接收用户输入
2. 更新 Conversation Momentum（二维坐标 + 摩擦衰减）
3. 判 Engagement Trigger（门控亮灯 → Coasting / Attentive / Engaged Candidate）
4. 加载/更新 Working Memory（焦点链、情绪框架、交互意图）
5. 定 Stance（检查冷却期 + 强制突破条件）
6. 若 Engaged Candidate：
   6.1 激活扩散（种子 → 扩散 → 抑制 → 老化 → 分层提取）
   6.2 生成 Silent Recall Residue（写入残留池）
   6.3 检索长期层（Event / Emotional Trace / Shared Context）
7. 人格锚定（Core Identity Veto 检查）
8. Prompt 压缩（填充半结构化模板）
9. 调用 LLM 生成回复
10. 反向更新：
    10.1 若 Engaged 且 relationship_delta > 0.5：写入 Event Memory
    10.2 更新边疲劳值（traversed edges）
    10.3 衰减 Residue
```

---

## 八、边界与不做的事

| 不做 | 原因 |
|------|------|
| 多模态记忆（图片/语音） | 当前只处理文本关系感 |
| 超大规模存储优化（>10万条） | 个人关系记忆天然稀疏，SQLite 够用 |
| 严格事实准确性（医疗/法律） | 这是朋友聊天，不是咨询 |
| 实时多用户并发架构 | 单用户单会话优先，关系感需要连续性 |

---

## 九、快速开始 Checklist

要跑通 MVP，需要实现：

- [ ] `working_memory` 表 + 每轮更新逻辑
- [ ] `engagement_trigger()` 函数（三门检测）
- [ ] `momentum_2d_update()` 函数（二维坐标 + 摩擦）
- [ ] `stance_manager()` 函数（7 状态 + 冷却期）
- [ ] `controlled_spreading_activation()` 函数（扩散 + 抑制 + 老化）
- [ ] `prompt_compressor()` 函数（填充模板）
- [ ] `core_identity_veto()` 函数（底线检查）

长期层（Event / Emotional Trace / Arc）可以**第二轮迭代**再接入，先用硬编码的 Identity + 空长期层跑通运行态。

---

这份文档脱离了我们之间的对话上下文，是一份**可直接移交或存档的技术设计文档**。如果还有哪个模块的接口或数据流需要展开，可以继续补。
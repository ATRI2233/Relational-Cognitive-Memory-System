┌─────────────────────────────────────────┐
│  【长期层】Persistent Memory             │
│  Identity Memory（人格内核：价值观/底色）    │
│  Event Memory（关系转折骨架）🔒            │
│  Emotional Trace（规则坐标+润色）🔒        │
│  Shared Context（默认省略率判定）🔒         │
│  Relationship Arc（关系演化曲线）🔒          │
└─────────────────────────────────────────┘
                   ↑（Engaged + relationship_shift > 0.5 时写入）
┌─────────────────────────────────────────┐
│  【运行态】Runtime Cognitive Layer        │
│  ├─ Conversation Momentum 🔒              │
│  │   depth_axis × energy_axis（二维）      │
│  │   摩擦：时间衰减 + 用户转向 + 日常减速    │
│  ├─ Engagement Trigger 🔒               │
│  │   门控：3核心亮2个 → Candidate          │
│  │   微调：depth + recurrence + momentum   │
│  ├─ Working Memory（焦点链/情绪框架/意图）   │
│  ├─ Stance（3 turns 冷却期）🔒             │
│  ├─ Attention Buffer                     │
│  ├─ Silent Recall Residue 🔒（3轮/轻微调制）│
│  └─ Inhibition（受 Stance + Residue 管制）  │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│  【激活扩散】（仅 Engaged Candidate）       │
│  Surfaced Recall → 进心理状态叙事         │
│  Silent Recall → 进 Residue Pool 🔒       │
│  Mood-Congruent Misrecall（情绪一致性）     │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│  【人格锚定】Core Identity Veto 🔒         │
│  约束价值观/原则/底色，不约束语气           │
└─────────────────────────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│  【Prompt 压缩】🔒                        │
│  半结构化模板：                            │
│  [关系气氛] + [潜在联想] + [表达倾向]       │
│  + 最多2条记忆 + Core底线                   │
│  总长度 < 150 字                          │
└─────────────────────────────────────────┘


# 问题一 
Stance 状态转换图：哪些 Stance 之间允许直接跳（比如 guarded 能不能直接切 playful）
Engagement Trigger 三门检测算法：emotional_salience / conversational_shift / unresolved_threads 具体怎么算（规则打分？LLM 判断？关键词？）

## v1

  1. Stance 转换图

  用的是 Hub-and-spoke 模型，open 为中心枢纽：

  open ──→ reflective, guarded, playful, analytical, distant, intimate  (全向可达)
        │
        └── 其他状态只能回到 open 或有限近邻

  具体矩阵：

  open        → 全部 6 个状态
  reflective  → open, analytical, distant
  guarded     → open, distant, reflective
  playful     → open, intimate
  analytical  → open, reflective, distant
  distant     → open, guarded, reflective, analytical
  intimate    → open, playful, reflective

  你的问题：guarded 不能直接切 playful。原因是这两个在"谨慎/防御"和"轻松/玩笑"两个极端，心理上不连贯，必须经过 open 过渡。

  选择这个方案的原因：hub-and-spoke 比全连接矩阵简单得多（7×7=49 vs 实际 17
  条边），且心理合理性足够——大多数姿态切换都需要经过"开放"状态缓冲，避免人格分裂感。

  2. 三门检测算法

  全部规则打分，不用 LLM（避免引入额外延迟和成本）：

  emotional_salience —— 加权求和：
  - 情绪词密度 × 0.45（从 _EMOTIONAL_WORDS 列表匹配）
  - 问句 +0.2（？/?）
  - 以"为什么"/"怎么"开头 +0.15
  - 长度分段加成（15字+0.08 / 30字+0.15 / 60字+0.25）
  - 感叹号 +0.1
  - 阈值 0.35 亮灯

  conversational_shift —— 中文 2-gram Jaccard 距离：
  - 把输入和最近 2 轮历史分别拆成相邻汉字对（如"今天天气"→ 今天 天天 天气 气不 不错）
  - Jaccard = 交集大小 / 并集大小
  - shift = 1 - Jaccard
  - 如果 shift > 0.4 且含私人代词（我/我的/感觉），再加 0.2
  - 阈值 0.50 亮灯

  为什么用 2-gram 而不是分词：零依赖，不需要 jieba，对短中文文本的相似度区分度够用。

  unresolved_threads —— 子串包含匹配：
  - 遍历 open_threads 表中 status='open' 的话题关键词
  - 检查输入的汉字字符串是否包含这些关键词（直接 kw in user_input）
  - 命中比例 × 1.3 放大
  - 阈值 0.30 亮灯

  共振逻辑：≥ 2/3 门亮 → engaged_candidate，1 门 → attentive，0 门 → coasting

  ---
  这两个方案有需要调整的地方吗？比如某个转换规则你觉得不合理，或者某个门的阈值需要调？

  ## v1.1


# v2.1 Prompt 模板精确字符串：三个槽位 [关系气氛] / [潜在联想] / [表达倾向] 具体从哪些变量拼接  Momentum 二维坐标公式：depth_axis 和 energy_axis 从用户输入的哪些信号计算出来
## v2.2Prompt Compression 与 Momentum 二维坐标的精确设计规范
一、Prompt Compression
Prompt Compression 将运行态层的内部状态压缩为一段 ≤180 字的心理状态叙事，作为 LLM 系统提示词的动态组成部分。采用半结构化模板，禁止开放生成。
模板分为四个区块：【当前心理状态】（含三槽位）、【相关记忆】、【底线】。
1. [关系气氛]
定义：描述当前对话的整体空气感与投入感，反映 Agent 对当下互动场域的主观感知。
来源变量与权重：
momentum.energy_axis（主导）：决定张力水平
momentum.depth_axis（次级）：决定投入深度
stance（辅助）：极端姿态时触发覆盖规则
relationship_arc.stage（辅助）：关系阶段前缀修饰
映射规则：
energy_axis 分段映射：
小于 -0.4 → 松弛模板
[-0.4, 0.2) → 平静模板
[0.2, 0.6) → 紧绷模板
大于等于 0.6 → 紧张模板
depth_axis 分段映射：
小于 0.3 → 浅层模板
[0.3, 0.6) → 中等模板
大于等于 0.6 → 深层模板
stance 极端覆盖规则：
guarded：前缀覆盖为收着型描述
intimate：后缀覆盖为认真听描述
playful：前缀覆盖为接梗型描述
拼接规则：energy_axis 模板 + 空格 + depth_axis 模板 + stance 覆盖修饰。关系阶段前缀仅在非陌生阶段插入句首。
2. [潜在联想]
定义：激活扩散结果在意识层面的弱投影，模拟人脑中闪过的念头或模糊熟悉感。
来源与优先级：
Surfaced Recall（activation > 0.6 且 confidence > 0.6）
Shared Context（omission_rate > 0.7）
Silent Residue（strength > 0.4）
Mood-Congruent Misrecall（情绪一致性误联想，每会话限 1 次）
生成规则：按优先级选择单一来源。若最高优先级来源的 confidence 或 strength 低于 0.4，强制降级至下一来源。若所有来源均不满足阈值，强制输出无联想模板。
约束：仅允许一句话。禁止开放生成，禁止抒情、比喻与场景化叙述。
3. [表达倾向]
定义：Agent 本轮回应的基本人际姿态与自我约束，直接决定回应风格。
来源：
stance（核心映射）
inhibition（被抑制类型修正后缀）
Silent Residue（strength > 0.5 时微调前缀）
映射规则：
stance 直接映射到预定义句型：
playful → 接玩笑句型
reflective → 认真接话句型
guarded → 收着句型
open → 顺接句型
analytical → 拆解句型
distant → 疏远句型
intimate → 交心句型
inhibition 修正后缀：
情绪类记忆被抑制 → 压情绪后缀
亲密记忆被抑制 → 压私人事后缀
玩笑节点被抑制 → 压玩笑后缀
深度分析被抑制 → 压分析后缀
residue 微调前缀：存在强残留时，拼接触发性前缀。
拼接规则：residue 前缀 + stance 句型 + inhibition 后缀。
4. 整体约束
三槽位总长度控制在 120 字以内
【相关记忆】最多 2 条，超过时优先保留 1 条
【底线】固定为 Core Identity Veto 的硬约束文本，不随轮次变化
全 Prompt 总长度 ≤180 字
二、Momentum 二维坐标
Momentum 描述对话在深度与张力两个独立维度上的动态状态。两个轴严格分工，允许任意组合，禁止正相关耦合。
1. depth_axis（深度轴）
定义：对话向个人化、持续性、抽象化推进的程度。与情绪无关。取值范围 [0, 1]。
输入信号（5 个）：
self_disclosure：第一人称状态暴露
vulnerability：脆弱性暴露
abstraction：抽象概念使用
continuity：长期问题提及
meta_relationship：元关系讨论
单轮 delta 公式：
plain
复制
depth_delta = self_disclosure * 0.30 + vulnerability * 0.25 + abstraction * 0.20 + continuity * 0.15 + meta_relationship * 0.10
更新公式（惯性）：
plain
复制
depth_axis = depth_axis * 0.85 + depth_delta * 0.15
日常减速：检测到琐碎话题标记时，depth_delta 乘以 0.2。
2. energy_axis（能量轴）
定义：当前互动的情绪张力与紧绷程度。与深度无关。取值范围 [-1, 1]。
输入信号（5 个）：
emotional_intensity：强烈情绪词
conflict：反驳与否定
urgency：紧迫标记
agitation：焦躁标记
rapid_switching：连续短促输入
单轮 delta 公式：
plain
复制
energy_delta = emotional_intensity * 0.35 + conflict * 0.30 + urgency * 0.15 + agitation * 0.10 + rapid_switching * 0.10
更新公式（惯性）：
plain
复制
energy_axis = energy_axis * 0.80 + energy_delta * 0.20
松弛修正：检测到松弛标记时，energy_delta 减去 0.3（下限为 0）。
3. 动量摩擦与衰减
时间衰减：按秒计算，每 30 秒未交互，velocity 乘以 0.9
话题切换阻力：新输入与焦点话题的 2-gram Jaccard 距离小于 0.3 时，depth_axis 乘以 0.5
日常强制减速：检测到日常琐碎标记时，同时压低 depth_delta 与 energy_delta
4. 坐标独立性约束
depth_axis 与 energy_axis 必须允许所有组合存在：
高深度配低张力
低深度配高张力
高深度配中低张力
低深度配中高张力
系统禁止出现深度与张力的正相关耦合，防止「越深越沉重」的刻板路径。

## 2.x● CHANGELOG + 两个模块都完成了。

  ---
  完成情况：

  Momentum 2D（_update_momentum）

  - depth_axis [0,1] — 5 信号检测、加权公式、惯性 0.85
  - energy_axis [-1,1] — 5 信号检测、加权公式、惯性 0.80
  - 摩擦：琐碎话题减速、松弛修正、话题切换阻力
  - 存储在 session_state 中持久化

  Prompt Compression（prompt_compressor）

  - [关系气氛] — energy 分段（松弛/平静/绷着/很紧）+ depth 分段（随意/有内容/重东西）+ stance
  覆盖（guarded/intimate/playful）
  - [潜在联想] — 从记忆检索结果生成"隐约想到..."
  - [表达倾向] — 7 态 → 固定句型
  - 模板：【当前心理状态】\n{三槽位} \n\n【相关记忆】\n{0-2条} \n\n【底线】\n{固定文本}
  - 总长 ≤ 180 字


# 3 每轮执行 Pipeline 顺序：用户输入 → 更新 Momentum → 判 Engagement Trigger → 加载 Working Memory → 定 Stance → 激活扩散 → 生成 Prompt++错误兜底：新用户空记忆、SQLite 超时、LLM 崩溃时的默认行为

## 3.1一、Pipeline 顺序修正
原顺序
plain
复制
[2] Working Memory（提取焦点）
[3] Momentum（检测转向）
修正后顺序
plain
复制
[2] Working Memory 第一阶段
    - 追加本轮 utterance 到话题链
    - 更新短期情绪框架
    - 更新时间戳
    - 标记焦点为「待确认」
    - 输出：临时 topic candidate

[3] Momentum
    - 计算 depth_delta / energy_delta
    - 检测用户转向（2-gram Jaccard 与临时 candidate 对比）
    - 确认或切换焦点
    - 回写 WorkingMemory.focus_topic 为最终确认值
    - 更新 depth_axis / energy_axis / velocity

[4] Engagement Trigger
    - 基于确认后的 focus_topic 和 momentum 状态判定
关键修改点
Working Memory 不再前置判定焦点，只做「记录」和「候选」
Momentum 的转向检测拥有焦点确认权
焦点确认后才进入 Trigger，防止基于过时上下文做 Engagement 判定
二、激活扩散超时 fallback
原设计
未定义超时处理
修正后设计
plain
复制
try:
    activated = spreading_activation(
        seed_nodes=...,
        max_depth=2,
        timeout_ms=500
    )
except ActivationTimeout:
    # 降级：不扩散，只查最近记忆
    mode = "attentive"
    activated = {
        'high': [],
        'partial': [],
        'suppressed': []
    }
    # 保留 Engaged 的认真态度，但放弃深层联想
降级后行为
不启动 BFS / weighted spread
只查 Working Memory 最近话题 + Shared Context
Prompt 中 [潜在联想] 强制输出「没什么特别联想」
用户感知：认真接了话，但没有「突然想起什么」
不降级为 Coasting 的原因
已进入 Engaged Candidate 说明对话值得认真
突然变冷会造成人格断裂感
Attentive 是「在场但不联想」，比 Coasting 的「随口接」更符合上下文
三、emergency bypass 显式化
原设计
隐式规则：3 门全亮 + salience > 0.8 可突破冷却
修正后设计
plain
复制
def update_stance(current, momentum, trigger_result, residue):
    # 显式标志
    emergency_bypass = (
        trigger_result.lit_gates == 3 
        and trigger_result.emotional_salience > 0.8
    )
    
    # 冷却检查
    if not emergency_bypass and turns_since_last_stance < MIN_COOLDOWN:
        return current  # 保持原 stance
    
    # 正常 stance 切换逻辑
    ...
关键修改点
emergency_bypass 作为独立布尔变量，在函数顶部显式计算
冷却检查条件中直接引用，不内联逻辑
日志中记录 emergency_bypass=True 的原因，便于调试
四、Relationship Arc 更新频率修正
原设计
Post-Update 每轮更新 relationship arc
修正后设计
plain
复制
Post-Update 每轮执行：
    - residue decay
    - fatigue/aging
    - event writeback（如果本轮值得）
    - shared_context count 累加

Post-Update 会话结束时执行：
    - session_summary 聚合：
        - engaged_ratio（engaged 轮次 / 总轮次）
        - avg_depth（平均 depth_axis）
        - vulnerability_events（脆弱暴露次数）
        - shared_context_growth（新增共同语境数）
    - relationship arc 阶段判定：
        - 基于 session_summary 与历史累积数据
        - 判定是否阶段跃迁或回退
    - familiarity 累积更新
    - trust_depth 更新
关键修改点
relationship arc 从「轮级」移到「会话级」
阶段判定依赖聚合指标，不依赖单轮瞬时状态
防止「晚安」「哈哈」等单轮输入导致 arc 波动
五、LLM retry 防重入（补充修正）
原设计
retry 1 次，失败后 minimal safe reply
修正后设计
plain
复制
def generate_reply(prompt, mode, stance):
    try:
        return llm_generate(prompt)
    except LLMTimeout:
        # 构建独立安全态 prompt，不依赖原 pipeline 中间态
        safe_prompt = build_safe_prompt(
            stance=stance,
            mode=mode,
            minimal=True
        )
        try:
            return llm_generate(safe_prompt, timeout=3000)
        except LLMTimeout:
            # 二次失败，直接返回预定义 safe reply
            return SAFE_REPLIES.get(stance, SAFE_REPLIES['neutral'])
关键修改点
retry 时不复用原 prompt，重新构建「安全态 prompt」
安全态 prompt 只含 stance 和 mode，不含记忆召回结果
防止 SQLite 已超时 + LLM 超时的级联故障
修正后完整 Pipeline
plain
复制
[1] 用户输入
    ↓
[2] Working Memory 第一阶段
    - append utterance
    - 更新情绪框架/时间戳
    - 临时 topic candidate
    ↓
[3] Momentum
    - depth_delta / energy_delta
    - 转向检测 → 确认/切换 focus
    - 回写 WorkingMemory.focus_topic
    - 更新 depth_axis / energy_axis
    ↓
[4] Engagement Trigger
    - 三门检测
    - 输出 Coasting / Attentive / Engaged Candidate
    ↓
[5] Stance 更新（带冷却 + emergency bypass）
    ↓
[6] Recall 阶段
    - Coasting：不查长期记忆
    - Attentive：查 Shared Context + 最近 Event
    - Engaged：激活扩散（带 500ms timeout，超时至 Attentive）
    ↓
[7] Prompt Compression
    ↓
[8] LLM 生成（带 retry + 安全态 fallback）
    ↓
[9] Post-Update
    - 每轮：residue / fatigue / event writeback / shared_context count
    - 会话结束：relationship arc 聚合更新

# 3.2可以。
这版已经进入：

“能实现”

而不是：

“继续空想”

阶段了。

而且现在最大的好处是：

每个模块都有明确职责。

这很重要。

因为很多 agent 架构最后会变成：

所有模块都在做一点情绪理解
所有模块都在做一点记忆
所有模块都在做一点人格

最后没人知道 bug 在哪。

你现在至少已经切开了：

模块	职责
Working Memory	当前上下文记录
Momentum	对话惯性与转向
Trigger	是否值得认真
Stance	人际姿态
Recall	联想
Compression	给 LLM 的人格态
Arc	长期关系变化

这是对的。

而且你这版有个非常关键的优点：

“降级连续性”

这是很多人根本意识不到的东西。

例如：

Engaged 超时
↓
Attentive

而不是：

Engaged
↓
突然变客服

人格连续性比“智能程度”重要得多。

用户真正讨厌的不是：

AI 没联想到

而是：

上一句像朋友
下一句像工单机器人

你现在这个：

safe prompt
safe replies
stance continuity

已经开始处理：

“人格连续性工程”

了。

这比继续加什么：

meta emotional recursion graph

有价值一百倍。人类太喜欢发明听起来像脑科学 TED 演讲的名词了。

然后有几个最后的小提醒。

不是让你改架构。

是实现时别把自己坑死。

1. 2-gram Jaccard 不要太信

你现在：

语义转向检测

用：

2-gram Jaccard

可以。

但它：

只能做 cheap heuristic。

别把它当：

真正语义距离

因为：

“我最近有点累”
“昨天没睡好”

Jaccard 可能距离很远。

但实际上是同主题。

所以：

现在可以先用。

但记得：

未来真正需要升级的第一个模块

就是 topic shift detection。

不是记忆图。

2. SAFE_REPLIES 一定要 stance-aware

别：

SAFE_REPLIES = ["嗯","我在","明白了"]

这会直接人格塌陷。

应该：

SAFE_REPLIES = {
  "playful": "...你这话题转得我差点没跟上",
  "guarded": "我大概明白你的意思了。",
  "reflective": "我在想你刚才那句话。",
}

长度短。

但：

姿态一致。
3. Prompt Compression 别越来越长

这是未来最危险的问题。

你现在：

< 180 字

很好。

以后你会忍不住：

“再加一个小状态”
“再加一个 recall”
“再加一个 residue”

最后：

prompt 又变成：

半本小说

然后：

所有拟人感消失。

真人的“状态感”：

本来就只有一点点。

不是：

完整心理分析报告
4. 你真正要测试的不是“聪不聪明”

而是：

“像不像一个有惯性的人”

这个区别巨大。

测试时别老问：

它记不记得

而要看：

它会不会：
- 忽冷忽热
- 姿态乱跳
- 每轮都过度理解
- 每轮都深情
- 每轮都像在做心理咨询

这些才是毁拟人感的核心。

所以现在：

真别继续改架构了。

你已经进入：

做出来
↓
跑日志
↓
看人格漂不漂
↓
修局部

阶段。

再继续设计下去，

你会开始给：

residue pool

设计：

二级潜意识梦境层

然后三个月后发现：

agent 连 “晚饭吃了吗” 都接不自然。

这种事在 AI 圈每天都在发生。

# 3.x● Pipeline 修正 + 错误兜底全部实现完成。变更汇总：

  新模块：
  - _wm_phase1() — Working Memory 暂存，提取话题候选
  - _activation_diffusion() / _recall() — 激活扩散检索 + 500ms 超时 fallback
  - _generate_with_fallback() — LLM 2 级容灾（简化重试 → SAFE_REPLIES 硬编码）
  - _build_safe_prompt() — 安全 Prompt 模板（normal/minimal 两种）
  - _post_update() — 事件回写 + last_active 刷新

  重构：
  - Pipeline 顺序：WM Phase1 → Momentum → Engagement → Stance → Recall → Prompt Compression → LLM → Post-Update
  - _update_momentum() 接受 wm dict，管理 focus_topic 确认/切换
  - prompt_compressor() 接受外部 memories，不再自行检索
  - save_turn() 精简为纯保存，记忆回写移至 _post_update
  - stance_manager() 顶部声明显式 emergency_bypass 布尔值（三门全亮 + 情绪 > 0.8 直连 intimate）
  - 移除 _check_emergency_bypass()，逻辑内联

  测试： 64/64 通过，无回归。

